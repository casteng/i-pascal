type Integer = Integer;
var
    a, b: Integer;
    s1: set of Integer;
const c = 1;
begin
    a := b div a;
    for <caret> do ;
    if <caret> do ;
    while <caret> do ;
    repeat
    until <caret>;
    s1 := [1, 2..5];
end.