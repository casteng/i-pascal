type Integer = Integer;
var
    a, b: Integer;
    s1: set of Integer;
begin
    a := b div a;
    for <caret> do ;
    if <caret> then ;
    while <caret> do ;
    case <caret> of

    end;
    s1 := [1, 2..5];
end.